{
  "name": "react-space-router",
  "description": "Space router bindings for React.",
  "version": "1.0.0-pre.1",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist",
    "src",
    "CHANGELOG.md",
    "MIGRATION.md"
  ],
  "sideEffects": false,
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/humaans/react-space-router.git"
  },
  "keywords": [
    "react",
    "browser",
    "clientside",
    "router",
    "history",
    "location",
    "url"
  ],
  "author": "Karolis Narkevicius",
  "scripts": {
    "test": "npm run build && oxlint --ignore-pattern demo --ignore-pattern dist --ignore-pattern docs && oxfmt --check --ignore-path=.gitignore --ignore-path=.oxfmtignore '**/*.{ts,tsx,js,css,yml}' && c8 ava",
    "format": "oxfmt --ignore-path=.gitignore --ignore-path=.oxfmtignore '**/*.{ts,tsx,js,css,yml}'",
    "demo": "npm run build && npm --prefix demo run dev",
    "demo:build": "npm run build && npm --prefix demo run build",
    "coverage": "c8 --reporter=html ava",
    "build": "rm -rf dist && tsc",
    "prepack": "npm run build",
    "watch": "tsc -w",
    "release": "np --no-release-draft",
    "release:beta": "np --tag=beta --no-release-draft",
    "docs:watch": "hugo server -s docs",
    "docs:build": "hugo -s docs",
    "release:docs": "npm run docs:build && gh-pages -d docs/public"
  },
  "dependencies": {
    "space-router": "^1.4.0"
  },
  "devDependencies": {
    "@swc-node/register": "^1.12.1",
    "@swc/core": "^1.15.43",
    "@types/react": "^19.2.17",
    "ava": "^8.0.1",
    "c8": "^12.0.0",
    "gh-pages": "^6.3.0",
    "jsdom": "^29.1.1",
    "oxfmt": "^0.59.0",
    "oxlint": "^1.74.0",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "typescript": "^6.0.3"
  },
  "peerDependencies": {
    "react": ">=18"
  },
  "ava": {
    "files": [
      "test/*.test.*"
    ],
    "extensions": [
      "ts",
      "tsx"
    ],
    "nodeArguments": [
      "--import=@swc-node/register/esm-register"
    ]
  },
  "allowScripts": {
    "@swc/core@1.15.43": true
  }
}
