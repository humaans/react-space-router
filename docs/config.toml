baseURL = "https://humaans.github.io/react-space-router"
languageCode = "en-us"
title = "React Space Router"
description = "A web router with all the essentials."
# single-page docs site — no tag/category pages
disableKinds = ["taxonomy", "term"]

[markup]
  [markup.highlight]
    noClasses = false
    tabWidth = 4
  [markup.tableOfContents]
    startLevel = 2
    endLevel = 3
    ordered = false
